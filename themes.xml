<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Presensi" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Presensi" parent="Base.Theme.Presensi" />

    <!-- Custom Style for DatePicker -->
    <style name="CustomDatePicker" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:datePickerMode">@color/primary_darkblue</item>
        <item name="android:colorAccent">@color/primaryblue</item>
        <item name="android:textColorPrimary">@color/black</item>

    </style>

</resources>